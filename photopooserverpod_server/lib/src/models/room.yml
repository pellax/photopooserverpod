class: Room
table: room
fields:
  name: String
  description: String
  city: String
  users: List<RoomsMembership>?,relation(name=users_membership)
  messages: List<Message>?,relation(name=room_message)